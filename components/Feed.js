import Posts from "./Posts";
import Stories from "./Stories";

function Feed() {
  return (
    <main>
      <section>
        <Stories />
        <Posts />
      </section>
      <section>
        {/* Mini Profile */}
        {/* Suggestion */}
      </section>
    </main>
  );
}

export default Feed;
